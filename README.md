1️⃣ Difference between var, let, and const
**Ans:** var is a global scope and also a functional scope if declear outside of a function. it can be reassigned 
and also redelcear in the same scope.
2️⃣ What is the spread operator (...)?
**Ans: ** the spreed operatior used to  copy or extend an element of an object or arry into another array or objects.
3️⃣ What is the difference between map(), filter(), and forEach()?
**Ans:**filter used to elements based on a condition and return a new array on the other hand the map find transform each element of an array
and return a new array but the forEach used to looping action and array but its cannot return a new array
4️⃣ What is an arrow function?
**Ans:** An Arrow function is a more realiable and more conicse its a shorted function.it was comming to the ES6 and makes the code cleaner and easier to read.
5️⃣ What are template literals?
**Ans:** the tamplate literals used to making a dynamic text and also crateting a HTML file.
